<div class="modal fade" id="<%= modal_name %>" tabindex="-1" aria-labelledby="<%= modal_name %>Label" aria-hidden="true">
  <div class="modal-dialog">
    <%= simple_form_for Contact.new, local: true do |f| %>
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="<%= modal_name %>Label">Sign Up For Emails & Get Two Weeks Free!</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <div class="card">
            <div class="card-body">
              <h2 class="featurette-heading mt-1">Get all these amazing features for one low price!</h2>
              <h3 class="text-muted">Sign up for your first two weeks free!</h3>
              <p>
                Our app isn't quite ready, yet. We want you to be the first to know when it is! 
                Please fill out this form to receive emails from use with the lastest news for 
                Teensy Bit and when you will be able to start your free two weeks!
              </p>
              <%= f.error_notification %>
              <%= invisible_captcha %>
              <%= f.input :name %>
              <%= f.input :email %>
              <%= f.input :opt_in_emails, as: :hidden, input_html: { value: true } %>
              <%= f.input :source, as: :hidden, input_html: { value: source } %>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
          <%= f.button :submit, "Submit", class: "btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>