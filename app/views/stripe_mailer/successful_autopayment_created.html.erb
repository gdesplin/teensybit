<h2><%= @name %>, you successfully began an autopayment plan with your daycare!</h1>
<p>
  Your payment method will be charged <%= number_to_currency(@stripe_price.amount) %> every <%= @stripe_price.recurring["interval_count"] if @stripe_price.recurring["interval_count"].to_i > 1 %> <%= @stripe_price.recurring["interval"].pluralize(@stripe_price.recurring["interval_count"].to_i) %>
</p>
<%= link_to "Sign In",  %i[new user session] %>
